# Public URL of a server-side proxy that holds AI_TOKEN and calls OpenRouter.
# Do not put AI_TOKEN or any provider token in this file or in GitHub Pages variables.
NEXT_PUBLIC_AI_PROXY_URL=https://your-ai-proxy.example.com/chat
