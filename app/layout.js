import "./globals.css";

export const metadata = {
  title: "HTX Số | Trung tâm điều hành",
  description: "Demo quản lý hợp tác xã số",
};

export default function RootLayout({ children }) {
  return <html lang="vi"><body>{children}</body></html>;
}
